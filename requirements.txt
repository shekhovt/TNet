# relimport: relative-import bootstrap (https://pypi.org/project/relimport/).
# Co-developers can `pip install -e /path/to/relimport` over this to use a local checkout.
relimport

# Core
torch>=2.7
torchvision
numpy
scipy
pyparsing
transformers
Pillow

# Plotting / analysis
matplotlib
adjustText      # used by experiments/exp_plotting.py for non-overlapping plot labels

# GPU device selection (used by device.py to auto-pick a free GPU)
nvitop
psutil

# Runtime dependencies pulled in by torch but not always present
mpmath<1.4      # sympy compatibility (torch >= 2.7 requires sympy which requires mpmath)
networkx        # required by torch
typing_extensions>=4.0  # required by torch >= 2.7

# Fast ImageNet data loading (optional; required only for ImageNet training).
# FFCV itself is not a plain pip dependency: its C++ extension is built from source against
# a system OpenCV and libjpeg-turbo rather than a PyPI wheel -- see "ImageNet: FFCV" in
# README.md. Its non-compiled Python deps *are* plain pip packages and belong here:
terminaltables
pytorch_pfn_extras
fastargs
assertpy
tqdm
opencv-python-headless  # not opencv-python: that wheel needs libGL, absent on headless machines
numba
# The `git clone` + `pip install -e .` build step is in README.md.

# Running the published test suite (energy_ECML/tests/): the validation behind the paper's
# energy numbers. Not needed to train or to profile a network.
pytest

# Model inspection / visualization (optional, used by utilities/)
# torchview
# torchviz
# torchscope
